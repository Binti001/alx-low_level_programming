#include "main.h"

/**
 *  print_last_digit - checks if the character is lowercase
 *  Return: returns 1 and 0 depending on condition
 *
 *  @i: a character arguement
 *
 *  Returns: 0
 */

int print_last_digit(int i)
{
	i = i % 10;
		return (i);
}
